export class Category {

    id: number;
    name: string;
    img: string;
    descripcion: string;

    constructor(id: number, name: string, img: string, descripcion: string) {
        this.id = id;
        this.name = name;
        this.img = img;
        this.descripcion = descripcion;
 
    }
}

